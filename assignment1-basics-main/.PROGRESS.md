# CS336 Assignment 1 进度笔记

最后更新:2026-09-08

## 环境

- WSL2 + NVIDIA GPU(**RTX 5060 Laptop GPU**);conda 环境 `cs336-basics`;作业目录另有一个 `.venv`(uv 管理,torch 2.11.0+cu130,`cuda.is_available()=True` 已实测)
- 所有命令在 `~/cs336/assignment1-basics-main` 目录下执行
- 注意:Codex 会话的沙箱屏蔽 GPU 设备(沙箱内 `nvidia-smi` 报错、`cuda.is_available()=False` 是假象),**GPU 相关运行要在用户自己的终端里执行**;CPU 冒烟测试可在沙箱内跑

## ⏸ 暂停点(下次唤醒直接从这里继续)

**本次唤醒补充(2026-09-08 晚)**:全仓测试仍绿(47 passed / 1 xpassed,已重建 tiktoken 离线缓存,否则 tokenizer 测试因沙箱断网失败);GPU 仍须在用户自己的终端跑。**新发现:`data/` 目录已不存在**(TinyStories/OWT 原始文本被删),2.7d 的 tokenize 步骤需要先重新下载数据(README 里的四条 wget 命令)。已确认 `experiments/TinyStoriesV2-GPT4-train_vocab10000.pkl` 完好:vocab 10000 / merges 9743 / `<|endoftext|>` id=256,max id=9999 → uint16 够用。下一步顺序:①重下 TinyStories train/valid(OWT 视 2.7a/b 需要再下)→ ②写 tokenize 脚本(2.7d)→ ③训练循环脚本(5.3)→ ④GPU 训练。

**tokenize 脚本已完成并通过验证(2026-09-08 晚)**:`cs336_basics/tokenizer_dataset.py`(用户自己实现;main 缺中间段、f-string `.f` 缺精度两处问题已指出并修好)。valid 集已跑:22.5MB → **5,461,224 token / 13.9s / 4.12 bytes/token**,`data/tinystories_valid_tokens.npy` 10.9MB;验证全过:dtype=uint16、id∈[10,9999]、`np.load(mmap_mode='r')` 可加载、整体 decode 与原文逐字节一致。train 集(2.2GB 已下载)按用户要求**暂不跑,留到最后一次性处理**;届时命令:`python cs336_basics/tokenizer_dataset.py experiments/TinyStoriesV2-GPT4-train_vocab10000.pkl data/TinyStoriesV2-GPT4-train.txt data/tinystories_train_tokens.npy`(预计 ~21min / ~5.1 亿 token / ~1GB npy)。下一步 = 训练循环脚本(5.3)。

**训练脚本已完成并通过冒烟(2026-09-08 晚)**:`cs336_basics/train.py`(用户自己实现,Codex 逐轮指出 bug:ArgumentParser 关键字、spe→eps、param_groups、log_interval、eval 传路径字符串/丢标签、checkpoint 路径、start_step、os.makedirs 等,均已修)。CPU 3 步冒烟过(初始 loss≈9.26=ln10000,正确);用户在自家终端 GPU 20 步冒烟过(loss 9.23→9.13,warmup 中 lr 仅 1e-5,符合预期)。**待办顺序**:①tokenize train(~21min)→ ②全量 20000 步(batch64×256×20000=3.2768 亿 token)→ ③lr sweep(≥1 个发散 run,最终 val loss≤1.45)→ ④decode 函数(Section 6)+ 生成 256 token → ⑤4 个 ablation(去 RMSNorm/post-norm/NoPE/SiLU-FFN)→ ⑥batch size sweep → ⑦OWT 主实验(需重下 owt 数据+重训 32k 词表,重活)→ ⑧书面题(Unicode/BPE 问答/tokenizer 问答/Transformer FLOPs 账/AdamW 内存账/SGD 玩具)+ experiment log → ⑨make_submission.sh。

**基线训练完成(2026-09-09 00:33)**:20000 步 / batch64 / lr 1e-3(warmup 2000、min 1e-5),**最终 val loss 1.3667 / ppl 3.92 ≤1.45 达标**;checkpoint 每 500 步存于 `experiments/checkpoints/`(每个 272MB,40 个共 ~11GB,可清理)。注意:训练时开了 torch.compile,checkpoint 的 state_dict 键带 **`_orig_mod.` 前缀**,加载时要去掉。

**decode 完成(2026-09-09 00:55)**:`cs336_basics/decode.py`(用户实现,Codex 指出 pk1 拼写、加载逻辑误放函数内、__main__ 无参调用、缺 no_grad/seed 等问题,均已修;CPU 验证生成连贯文本)。要点:模型重建用 context_length=1024(>256,因 RoPE cos/sin 非持久化、load 时自动重建,绕开生成长度限制);temperature/top-p 采样、遇 id=256 停。剩余小瑕疵:ckpt 路径应改 `args.checkpoint`、循环内改用 `endoftext_id`。**当前待办**:lr sweep(3分)→ batch size sweep(1分)→ 4 ablation(4分)→ OWT(2分)→ 书面题。

**Tokenizer 类已完成**(讲义 Section 2.6):`cs336_basics/tokenizer.py` + `tests/adapters.py` 的 `get_tokenizer` 已接线,`pytest tests/test_tokenizer.py` **24 passed / 1 xfailed**(xfail=encode 超 1MB 内存,设计内)。BPE 训练全部完成。

**`get_batch` 也已完成**:`tests/adapters.py` 的 `run_get_batch`(numpy 随机起点 + 切片错一位),`pytest tests/test_data.py` **1 passed**。

**神经网络基础件也已完成**:`tests/adapters.py` 的 `run_softmax` / `run_cross_entropy` / `run_gradient_clipping`,`pytest tests/test_nn_utils.py` **3 passed**。

**模型基础算子已完成**(`run_linear` / `run_embedding` / `run_swiglu` / `run_rmsnorm` / `run_silu`),`tests/test_model.py` 目前 5 passed / 8 未实现。

**注意力组已完成**(`run_rope` / `run_scaled_dot_product_attention` 含 4D 版 / `run_multihead_self_attention` 无、有 RoPE),`tests/test_model.py` 目前 10 passed / 3 未实现。关键约定(已对 snapshot 验证):SDPA 的 mask **True=保留、False=屏蔽**;MHA 内部自带因果 mask;RoPE 按头维 d_k 旋转 Q、K(偶位减 sin、奇位加 sin)。

**模型组件全部完成**(组装组 `run_transformer_block` / `run_transformer_lm` 含截断输入已过),`tests/test_model.py` **13 passed**;全仓目前 44 passed / 3 未实现。pre-norm 结构:`h = x + MHA+RoPE(rmsnorm(x, ln1))`、`out = h + SwiGLU(rmsnorm(h, ln2))`;LM = 查表 → num_layers 个 block(按 `layers.{L}.` 前缀挑权重并剥前缀)→ `rmsnorm(ln_final)` → `@ lm_head.T`(无 softmax);rmsnorm eps 用 1e-5。

**优化器已完成**:`get_adamw_cls`(AdamW,解耦权重衰减)+ `run_get_lr_cosine_schedule`(线性 warmup + 余弦退火),`pytest tests/test_optimizer.py` **2 passed**。AdamW 要点(已对 snapshot 验证):更新段包 `with torch.no_grad()`;m/v 状态存 `self.state[p]`(首见 `zeros_like` 初始化,step 从 0 起);denom = √(v/(1−β₂ᵗ)) + eps;step_size = lr/(1−β₁ᵗ);`p.addcdiv_(m, denom, value=-step_size)` 后 `p.mul_(1 - lr*weight_decay)`(衰减在梯度更新之后)。

**checkpoint 也已完成**:`run_save_checkpoint` / `run_load_checkpoint`(`torch.save` / `torch.load` + `model.load_state_dict` / `optimizer.load_state_dict`),`pytest tests/test_serialization.py` **1 passed**。checkpoint 字典存 `model_state_dict` / `optimizer`(state_dict)/ `iteration` 三样;load 返回 `iteration`。全仓适配器函数至此全部实现完毕,整套 **47 passed / 1 xpassed**(xfail=encode 超 1MB 内存,设计内)。

**训练前置工作已开始:模型类(nn.Module)**。适配器里的 `run_*` 全是纯函数(不持有参数),训练需要真正的带参数模型,现在正在 `cs336_basics/model.py` 里按讲义 Section 3 把各类建起来。已完成:

- [x] `Linear`(3.3.2):`nn.Parameter` 形状 `(out, in)`,`trunc_normal_` σ²=2/(d_in+d_out) 截断 ±3σ,forward=`x @ W.T`,无 bias;已和 `run_linear` 数值对拍一致
- [x] `Embedding`(3.3.3):参数 `(num_embeddings, embedding_dim)`,`trunc_normal_` σ=1 截断 ±3,forward=整数索引查表;已和 `run_embedding` 对拍一致(最大差 0.0)
- [x] `RMSNorm`(3.4.1):唯一参数 `weight` 形状 `(d_model,)` 初始化全 1;forward 对**输入 x** 沿最后一维求 rms 后归一化再乘 gain;**upcast 到 float32 计算、最后转回原 dtype**(讲义考点);fp32 与 `run_rmsnorm` 对拍一致、bf16 输入输出保持 bf16
- [x] `RoPE`(3.4.3,用户自己实现):角度表 `register_buffer(persistent=False)`、查表后 `.to(x.dtype)`、偶奇成对旋转、stack+reshape 交错恢复;与 `run_rope` 对拍 0 误差;seq 用 `x.shape[-2]` 取(2D/3D/4D 通吃),2D token_positions 自动 unsqueeze(1) 对齐 heads
- [x] `MultiHeadSelfAttention`(3.4.5,用户自己实现):4 个 Linear(q/k/v/output_proj)+ 1 个 RoPE;分头 view+transpose、只对 Q/K 套 RoPE、tril mask + softmax + @V、transpose/contiguous/view 拼头 + output_proj;与 `run_multihead_self_attention_with_rope` 对拍 1D/(1,seq)/None 均 0 误差,batch≠heads 的 2D 位置正常,梯度正常,4 个 Linear 共 256 参数正确注册
- [x] `SwiGLU`(3.4.2,用户自己实现):w1 门控(F.silu)/w3 信息/w2 回投影,`w2(silu(w1(x))*w3(x))`,w2 不激活;与 `run_swiglu` 对拍 0 误差,权重形状 (d_ff,d_model)/(d_model,d_ff) 正确,梯度正常
- [x] `TransformerBlock`(3.5,用户自己实现):`attn`/`ln1`/`ln2`/`ffn` 四个子模块 + 两步 pre-norm 残差;fixture 真实权重对拍全长度/截断输入均 <2e-6(容差 1e-4);state_dict 9 个键形状全部正确,总参数 41088
- [x] `TransformerLM`(3.6,用户自己实现):`token_embeddings`(Embedding)+ `nn.ModuleList` 装 3 个 Block + `ln_final`(RMSNorm)+ `lm_head`(Linear(d_model,vocab_size),无 softmax);fixture 真实权重对拍全长度 1.05e-5、截断 9.5e-6(容差 1e-4);30 个键、参数总数 1403328 与手算一致。**模型类全部完成**

**当前位置(2026-09-08 本次会话)**:模型类 6 个基础件 + `TransformerBlock` + `TransformerLM` 全部完成并逐一对拍通过(全部 7 个类)。LM 这一轮犯的典型错误:漏 `super().__init__()`、`self.layers` 先存 int 再被单个 Block 覆盖、forward 遍历不可迭代的模块、`token_embedding` 少个 s、device/dtype 没透传,均已纠正;剩余小瑕疵:`layers`/`ln_final`/`lm_head` 的构造里 device/dtype 仍写死 None(训练脚本若用 `model.to(device)` 整体迁移则无碍,但建议改成透传保持一致)。**下一步 = tokenize 数据集(2.7d)**:TinyStories train/valid → `uint16` numpy 数组;然后训练循环脚本(5.3)→ GPU 训练实验。

**协作约定(重要)**:用户明确要求模型类代码**自己动手写**,Codex 只负责详细讲解、答疑、跑对拍验证,不要代写代码。下次唤醒沿用此模式。

模型类全部完成后:tokenize 数据集(Section 2.7d,TinyStories 10k 词表 → `uint16` numpy 数组)→ 训练循环脚本(Section 5.3)→ 训练实验。GPU 已确认可用(RTX 5060 Laptop),训练走 `cuda` 设备,不是 CPU 低资源方案。

Tokenizer 实现要点(已完成,供回顾):merge 语义对齐 tiktoken Rust 实现——每轮找 rank 最小的相邻 pair、**只合并最左一次出现**;特殊 token 用正则切分、按长度降序排列(处理重叠 token);`encode_iterable` 两级排水(完整特殊 token 直接发射 → 普通文本排水,保留最后一个 regex match + `max_special_len-1` 尾巴,避免 chunk 边界切断单词/特殊 token)。预分词复用 v4 的 `GPT2_SPLIT_REGEX`,与 tiktoken 的 `r50k_pat_str` 在连续空格/制表符等边角上已逐 id 对拍一致。

环境要点(重要):`uv run pytest` 会尝试联网同步依赖,当前网络受限会失败;**直接用 `.venv/bin/python -m pytest` 跑**。tiktoken 离线缓存已用本地 fixture 重建(哈希与官方一致):`/tmp/data-gym-cache/6d1cbeee…`(vocab.bpe,由 `gpt2_merges.txt` + `#version: 0.2` 头拼出)、`/tmp/data-gym-cache/6c7ea1a7…`(即 `gpt2_vocab.json`)。**重启 WSL 后 /tmp 会清空(已实测丢失过),重建命令:** `printf '#version: 0.2\n' > /tmp/vocab.bpe && cat tests/fixtures/gpt2_merges.txt >> /tmp/vocab.bpe && mkdir -p /tmp/data-gym-cache && cp /tmp/vocab.bpe /tmp/data-gym-cache/6d1cbeee0f20b3d9449abfede4726ed8212e3aee && cp tests/fixtures/gpt2_vocab.json /tmp/data-gym-cache/6c7ea1a7e38e3a7f062df639a5b80947f075ffe6`(在仓库根目录执行,无需联网)。

遗留(暂缓,用户说先不管):OWT 32k 实际被 OOM 杀过,`.wslconfig` 缺 `[wsl2]` 段头导致 `memory=32GB` 未生效,VM 仍是 7.6GB;待宿主机内存确认后补段头 + `wsl --shutdown`。

## 当前状态

- [x] 环境搭建、数据下载(`data/` 约 14GB,已加入 .gitignore)
- [x] `train_bpe` 朴素版实现(`cs336_basics/train_bpe_version1.py`),3 个单元测试全过(9.74s)
- [x] Tokenizer 类:encode/decode/encode_iterable(讲义 Section 2.6,`get_tokenizer` adapter 已接,`tests/test_tokenizer.py` 24 passed / 1 xfailed;含与 tiktoken 逐 id 对拍、重叠特殊 token、流式 5M 内存用例)
- [~] `train_bpe` 性能优化:增量计数/位置索引版(`train_bpe_version2.py`)完成,官方 3 测试全过(套件 1.74s,adapters 已接 v2);多进程预分词 + 流式读取待做(视 10k 实验是否达标)
- [x] TinyStories BPE 训练实验(vocab=10000)完成(v4 多进程版):总耗时 1:26.6(86.6s,1545% CPU)、峰值内存 201MB、len(vocab)=10000、len(merges)=9743;adapters 与驱动脚本均已接 v4,<2min 目标达成
- [x] OpenWebText BPE 训练实验(vocab=32000)——按用户指示视为成功(实际被 OOM 杀;`memory=32GB` 缺段头未生效,留待后续处理)
- [x] 定位 OWT 32k 第三次 OOM 根因:`/mnt/c/Users/xuyue/.wslconfig` 只有一行 `memory=32GB`、**缺少 `[wsl2]` 段头**,整份配置被 WSL 静默忽略 → VM 仍是默认 7.6GB(VM 于 Sep 3 00:38 重启过,说明配置在启动时被读取但被丢弃)。后续:视宿主机内存决定是否补段头 + `wsl --shutdown` 重启
- [x] `get_batch`(`tests/adapters.py` 的 `run_get_batch`),`tests/test_data.py` 1 passed
- [x] 神经网络基础件(`run_softmax` / `run_cross_entropy` / `run_gradient_clipping`),`tests/test_nn_utils.py` 3 passed
- [x] 模型基础算子(`run_linear` / `run_embedding` / `run_swiglu` / `run_rmsnorm` / `run_silu`),`test_model.py` 对应 5 项全过
- [x] 注意力组(`run_rope` / SDPA 3D+4D / MHA 无、有 RoPE),`test_model.py` 累计 10 项全过
- [x] 组装组(`run_transformer_block` / `run_transformer_lm` 含截断),`test_model.py` 13 项全过
- [x] 优化器(AdamW + cosine 调度),`test_optimizer.py` 2 passed;全仓 46 passed / 1 xpassed / 1 未实现
- [x] checkpoint(`run_save_checkpoint` / `run_load_checkpoint`),`test_serialization.py` 1 passed;全仓 47 passed / 1 xpassed
- [x] 模型类(nn.Module,`cs336_basics/model.py`):Linear/Embedding/RMSNorm/RoPE/MHA/SwiGLU/TransformerBlock/TransformerLM 全部完成,fixture 权重逐一对拍通过(Block <2e-6、LM 全长度 1.05e-5 / 截断 9.5e-6;LM 参数总数 1403328 与手算一致)
- [ ] tokenize 数据集(TinyStories train/valid → `uint16` numpy 数组,讲义 2.7d)
- [ ] 训练循环脚本及训练实验(讲义 Section 5.3 / 7;GPU 版,RTX 5060 Laptop)

## 关键约定

- 初始词表 = 256 个字节 + 特殊 token;merge 次数 = vocab_size - 256 - len(special_tokens)
- tie-break:频次高优先,同频次取字典序更大者
- 预分词用 `regex` 包;特殊 token 是硬边界,不参与 merge 计数
- 返回契约:`vocab: dict[int, bytes]`,`merges: list[tuple[bytes, bytes]]`
- 测试只认 `tests/adapters.py` 里的函数,每完成一个模块都要接线

## 待办 / 下一步

- OWT 全量(12GB/vocab=32000)再次 OOM(exit 137,8 分钟处被杀):`/usr/bin/time` 显示的 2.9GB 只是主进程,24 个 worker 子进程的内存不计入;实查系统已触顶——`Committed_AS 6.0GB > CommitLimit 5.8GB`、swap 仅剩 199MB,37% CPU/59 万次上下文切换=在颠簸。对策排序:①查 Windows 宿主机内存,有余量就 `.wslconfig` 设 `memory=12~16GB` + `wsl --shutdown` 重启(讲义允许 ≤30GB,零代码改动);②代码侧压缩(词表 key 用 bytes 代替字节元组 ~10x;节点值用 int token id 代替 bytes 对象);③worker 数降到 8~12。改完先跑 owt_valid 对拍(上一步被跳过)再上全量
- OWT 32k 第三次运行:v4 + 默认 12 worker,14:55 处被杀(exit 137),主进程峰值 RSS 5.3GB(不含 12 个 worker 的内存)。当前 VM MemTotal 仍为 7.6GB,swap 2GB。`.wslconfig` 已写入 `memory=32GB` 但**缺少 `[wsl2]` 段头** → 配置被忽略,证据:文件改于 Aug 26,VM 却于 Sep 3 00:38 重启(up 1d1h41m)后仍是默认值。补段头 + 按宿主机实际内存设值 + `wsl --shutdown`。另:此次 `tee` 后没接文件名,日志没落盘
- 下一步:①先 `owt_valid.txt`(277MB,行内特殊 token)对拍 v3 vs v4 的 word_counts/merges(vocab 取小如 5000 控制 v3 耗时),确认半行截断/行内特殊 token 无重复计数;②再跑 `owt_train.txt` 12GB vocab=32000,盯峰值内存(词类型比 TinyStories 大 1~2 个量级,节点/pair 结构可能数 GB,机器仅 7.6GB,必要时升 WSL2 内存或压缩表示);③Tokenizer 类可同时开工
- v4 多进程版官方 snapshot 测试失败(merges[627] 分叉),已定位:`find_chunk_boundaries` 把边界放在「特殊 token 所在行的行尾」而非「特殊 token 之后」,把特殊 token 后的换行从下一段剥离;实测 5M 样本上损失 1 个 `b'\n\n'` token、多出 2 个 `b'\n'`(608→607 / 35619→35621)。另有新行回退分支会在文档中间乱切。修法:①边界=特殊 token 结束位置(after_st);②删掉 newline 回退;③worker 读到越过 end 的行时截断到 end(否则行内特殊 token 的尾巴被重复计数);修完用 5M 样本对拍 v3 vs v4 的 word_counts 应零差异
- 决定先做多进程预分词。OWT 格式已查:特殊 token 是**行内**出现(共 2,355,962 处,如 `...announced.<|endoftext|>Close`),v3 的按行 split 已能处理;分块策略=预扫原始字节找到所有特殊 token 位置,把文件切成 ~24 段、每段边界恰在特殊 token 之后,worker 各自流式统计局部 Counter,主进程求和后交给不变的单进程 merge 循环;注意:多进程只省时间,OWT 的词类型大 1~2 个量级,最终 Counter/节点/pair 结构可能数 GB,仍需盯 7.6GB 内存
- 下一步两线并行:①OpenWebText 实验(`data/owt_train.txt` 12GB,vocab=32000,merge 数 31743)——单线程按比例约 45min,若讲义硬限仍是 30min 则必须上多进程预分词;且 OWT 词类型比 TinyStories 大 1~2 个数量级,节点/pair 结构可能数 GB,盯 `Maximum resident set size`(机器仅 7.6GB,必要时升 WSL2 内存或压缩表示);②Tokenizer 类(encode/decode,Section 2.6)+ 接 `get_tokenizer` + 跑 `test_tokenizer.py`
- 第二次 OOM 根因已查明:`cs336_basics/train_tinystories.py` 的 import 仍是 **version2**(整段 `f.read()`),adapters 已切 v3 但驱动脚本没切。实测 v3 流式预分词跑完整 2.1GB train:**480s / 峰值仅 28MB / 词类型 59887 / 总字节位置 434773** → 节点与 pair 结构也只有几十 MB,内存根本不是问题。动作:①驱动脚本 import 切到 v3 重跑(预计 ~8-9min、内存 <1GB,满足硬限);②打 <2min 目标再做多进程预分词(24 核,480s 可压到 ~30s)
- 验证策略改为「v3 为正式实现 + v1 为 oracle」,不再用 v2 对拍:①adapters import 切到 v3,官方 3 测试连跑 3 次确认 snapshot 稳定;②`diff_v1_v3.py` 对拍 corpus.en(500)、tinystories_sample_5M(1000)、valid(1000);③玩具阶梯 vs v1;全绿后跑 train 全量
- v3 的确定性修复已验证正确:Node 加 `word_id/pos`、`nodes_to_merge` 按 `(word_id,pos)` 排序后,在 corpus.en(500)与 tinystories_sample_5M(1000)上与 v1 的 merges 完全一致,且连跑两次结果相同;剩余动作:把同样三处改动移植到 v2(否则 v2 仍随机,diff 永远分叉),然后 v2/v3 diff 全绿 + 官方 3 测试稳定通过
- 流式 v3 已修通:预分词结果与 v2 完全一致(valid 上 13111 种 token 逐项计数相同);但 diff 仍在 `merges[907]` 分叉——根本原因在 **merge 循环自身的非确定性**:`pair_positions` 用 set 存 `(Node,count)`,Node 按内存地址哈希,迭代顺序每次运行都不同;当选中的 pair 在同一词内相邻出现(如 `aaa` 的 `(a,a)`)时,存活下来的 merge 位置随集合顺序而定 → 实测 v2 自己两次独立运行在 `merges[5477]` 分叉,15 次 `aaa` 微型实验 7:8。修复方向:给 merge 位置一个稳定顺序(按词内位置排序),复刻 v1「从左到右、不重叠」语义;修完验证:同一输入连跑两次必须逐字节一致 → v2/v3 diff 全绿 → 官方 3 测试(含 snapshot,此前是 flaky)稳定通过
- `train_bpe_version3.py`(逐行流式预分词)已写完,验证脚本 `cs336_basics/diff_v2_v3.py`;待办:在 valid 上确认 v2/v3 完全一致后,把驱动脚本 import 切到 v3 跑 train 全量看内存
- 新建 `train_bpe_version3.py`(复制 v2,只把预分词改成流式):数据格式为 story 多行、`<|endoftext|>` 单独成行;块边界用 carry 尾巴处理;先在 valid 上验证与 v2 的 merges 完全一致,再跑 train 看内存,最后加多进程(Pool,12~24 worker,进程间只传 Counter)
- TinyStories 实验首测:valid 集冒烟通过(11.0s / 峰值 120MB / 长度 10000+9743 正确);train 全量(2.1GB)被 OOM kill(exit 137,峰值 RSS 6.77GB,机器仅 7.6GB RAM)→ 必须先做「流式预分词 + 多进程预分词」再重跑;顺带修 tee 换行语法(管道与 `2>&1` 需同一行)
- 大语料前先做一轮随机差分对拍 v1(vocab 300~800、几十组),防边角回归
- `train_bpe_version2.py`(位置索引版)已通过玩具用例(构造、词尾/词首 merge、重叠出现、空计数、tie-break);下一步:随机差分对拍 v1 → 接官方 `test_train_bpe` → TinyStories valid 差分
- 先做 BPE 的「简易版增量计数」,验证正确后再考虑位置索引版
- 用 TinyStories valid 集当 debug 数据,确认优化前后结果完全一致

## 常用命令

- `uv run pytest`
- `uv run pytest tests/test_train_bpe.py::test_train_bpe`
- `bash make_submission.sh`
